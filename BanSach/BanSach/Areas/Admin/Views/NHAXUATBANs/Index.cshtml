@model IEnumerable<banSach.Models.NhaXuatBan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<main>
    <div class="container-fluid px-4">
        <br />
        <h2>Danh sách nhà xuất bản</h2>

        <div class="my-3">
            @Html.ActionLink("Thêm mới", "Create", null, new { @class = "btn btn-primary" })
        </div>

        <div class="card mb-4">
            <div class="card-body">
                <table id="datatablesSimple" class="table table-bordered table-hover">
                    <thead class="table-light">
                        <tr>
                            <th>Mã nhà xuất bản</th>
                            <th>Tên nhà xuất bản</th>
                            <th>Địa chỉ</th>
                            <th style="width: 150px;">Chức năng</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.MaNXB)</td>
                                <td>@Html.DisplayFor(modelItem => item.TenNXB)</td>
                                <td>@Html.DisplayFor(modelItem => item.DiaChi)</td>
                                <td>
                                    <div class="d-flex">
                                        <a href="@Url.Action("Edit", new { id = item.MaNXB })" title="Sửa">
                                            <i class="fa-solid fa-lg fa-pen-to-square"></i>
                                        </a>
                                        <div class="ms-2"></div>
                                        <a href="@Url.Action("Details", new { id = item.MaNXB })" title="Chi tiết">
                                            <i class="fa-solid fa-lg fa-circle-info"></i>
                                        </a>
                                        <div class="ms-2"></div>
                                        <a href="@Url.Action("Delete", new { id = item.MaNXB })" title="Xóa">
                                            <i class="fa-regular fa-lg fa-trash-can"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</main>